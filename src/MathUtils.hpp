#pragma once

namespace MathUtils {

// Простая функция, которую мы будем тестировать.
// Она намеренно примитивна, чтобы фокус был на самом процессе тестирования.
inline int Sum(int a, int b) {
    return a + b;
}

} // namespace MathUtils